# JDBC URL (Postgres for this example)
db.url=jdbc:postgresql://localhost:5432/testdb
db.user=test
db.password=secret
db.driver=org.postgresql.Driver

# Hikari pool settings
db.poolSize=5
db.connectionTimeoutMs=30000
db.initFailTimeoutMs=10000
# Optional validation query (useful for some DBs)
# db.validationQuery=SELECT 1